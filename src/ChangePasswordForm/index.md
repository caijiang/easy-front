## ChangePasswordForm

修改密码的表单

```tsx
import React from 'react';
import { ChangePasswordForm } from '@caijiang/easy-front';

export default () => (
  <ChangePasswordForm
    handler={(it) => {
      console.log(it);
    }}
  />
);
```

<API/>
